# Claude Code specific mise configuration
# Load with: MISE_ENV=claudecode mise install
# Or set in Claude Code settings/hooks to auto-load

[settings]
# Disable libgit2 and gix to prevent Flutter installation errors
# These settings prevent the "Failed to configure the transport" error
# when mise tries to clone the Flutter plugin repository
libgit2 = false
gix = false
